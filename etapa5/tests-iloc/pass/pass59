var store := inteiro,

f -> inteiro := [
  var loops := inteiro
  store := 6
  loops := 6
  enquanto(loops) [
    se (store) [
      store := store + (-1)
    ]senao[
      store := store + 1
    ]
    loops := loops + (-1)
  ]
  retorna store := inteiro
];
