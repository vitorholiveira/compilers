// Teste de Regressão: IF-ELSEs sequenciais com blocos vazios
// Teste para garantir que múltiplos IF-ELSEs sequenciais funcionam mesmo com blocos vazios
// Deve gerar jumpI e rótulos corretos para cada IF-ELSE

main -> inteiro := [
    var x := inteiro
    x := 5
    // Sequência de IF-ELSEs, alguns com blocos vazios
    se (x < 10) [
    ] senao [
        x := 1
    ]
    se (x > 0) [
        x := 10
    ] senao [
    ]
    se (x == 10) [
    ] senao [
        x := 2
    ]
    se (x != 0) [
        x := 20
    ] senao [
    ]
];

