// Comentários
// Este arquivo testa todos os tokens do analisador

// Palavras reservadas
tipo var senao decimal se inteiro retorna enquanto com

// Identificadores válidos
abc _def a1b2c3 _123abc zz99

// Literais inteiros
0 42 123456 000000123

// Literais decimais
0.1 00.01 123.000 000123.456 9.99

// Operadores compostos
<= >= == != := ->

// Caracteres especiais
! * / % + - < > [ ] ( ) = , ; & |

// Mistura de operadores e identificadores
x := y + 10;
a1 -> b2;

// Estruturas de controle
se (x <= y) 
    retorna x;
 senao 
    retorna y;


enquanto (a123 != d) 
    a123 := a123 - 1;


com (x, y, z) 
    retorna x + y + z;


// Espaços e quebras de linha
    tipo
var
se

// Casos de erro léxico
@ # $ ^ ~
